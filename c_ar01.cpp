#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
using namespace std;

int main() {
  vector<vector<int>> vec(100,vector<int>(100,0));
  int num;
  char temp;
  int count = 0;
  while(cin>>num)
  return 0;
}